<!-- 这里是关于主对话窗口的表单一 --><!-- 包括旧对话记录和新用户输入 -->document.addEventListener('DOMContentLoaded', () => {    const form = document.getElementById('main-chat-form');    form.addEventListener('submit', async (event) => {        event.preventDefault();        console.log("表单提交，准备连接后端...");        // 后续将用 fetch 提交并刷新 chat-window    });});<!-- 这里是prompt preset的事件监听 -->document.addEventListener('DOMContentLoaded', () => {    // 表单监听    const promptForm = document.getElementById('prompt-form');    promptForm.addEventListener('change', (e) => {        const selected = promptForm.prompt_option.value;        console.log("选择的 prompt:", selected);        // 你可以在这里触发异步更新或存储选择状态    });    const editButton = document.getElementById('prompt-edit-btn');    editButton.addEventListener('click', () => {        alert("你点击了编辑按钮！（后续可弹出窗口）");    });});